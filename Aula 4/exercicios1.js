const numeros = [5, 12, 11, 22, 23, 47, 55, 124, 1582, 2548];

for (let i = 0; i < numeros.length; i++) {
        const numero = numeros[i];
        if (numero % 2 === 0);
        console.log(numero);
}
